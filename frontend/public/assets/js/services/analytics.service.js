import api from "../core/api-client.js";const valid=n=>{if(!Number.isInteger(Number(n))||Number(n)<=0)throw new Error("Cửa hàng không hợp lệ");return Number(n)};export const getRevenue=id=>api.get(`/analytics/revenue?store_id=${valid(id)}`).then(r=>r.data);export const getSummary=id=>api.get(`/analytics/summary?store_id=${valid(id)}`).then(r=>r.data);export const getDailyRevenue=id=>api.get(`/analytics/revenue/daily?store_id=${valid(id)}`).then(r=>r.data);export const getTopProducts=(id,limit=5)=>{if(!Number.isInteger(Number(limit))||limit<1||limit>50)throw new Error("limit không hợp lệ");return api.get(`/analytics/top-products?store_id=${valid(id)}&limit=${limit}`).then(r=>r.data)};
