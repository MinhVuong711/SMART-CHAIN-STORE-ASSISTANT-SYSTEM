export function header(user,stores,selected){const admin=user?.role==="admin",logged=!!user;const store=stores.find(s=>String(s.id)===String(selected));return `<header class="topbar"><button class="btn btn-light mobile-toggle me-3" id="menu-toggle" aria-label="Mở menu"><i class="bi bi-list"></i></button><nav aria-label="breadcrumb"><ol class="breadcrumb mb-0"><li class="breadcrumb-item">Trang chủ</li><li class="breadcrumb-item active">Tổng quan</li></ol></nav><div class="ms-auto d-flex align-items-center gap-3">${!logged?"":admin?`<label class="visually-hidden" for="store-select">Chọn cửa hàng</label><select id="store-select" class="form-select form-select-sm" style="max-width:210px"><option value="">${stores.length?"Chọn cửa hàng":"Chưa có cửa hàng"}</option>${stores.map(s=>`<option value="${s.id}" ${String(s.id)===String(selected)?"selected":""}>${s.name}${s.address?` — ${s.address}`:""}</option>`).join("")}</select>`:`<span class="small text-muted"><i class="bi bi-shop me-1"></i>${store?.name||"Chưa được phân công cửa hàng"}</span>`}<div class="avatar">${logged?(user.username?.[0]?.toUpperCase()||"<i class='bi bi-person'></i>"):"<i class='bi bi-person'></i>"}</div><div class="d-none d-sm-block"><div class="small fw-semibold">${logged?(user.username||"Người dùng"):"Chưa đăng nhập"}</div>${logged?`<div class="text-muted" style="font-size:.72rem">${user.role==="admin"?"Admin":"Nhân viên"}</div>`:""}</div><button class="btn btn-sm btn-outline-secondary ${logged?"":"d-none"}" id="logout" aria-label="Đăng xuất"><i class="bi bi-box-arrow-right"></i></button></div></header>`}
