export function toast(text,type="info"){const el=document.createElement("div");el.className=`toast show align-items-center text-bg-${type} border-0`;el.setAttribute("role","status");el.innerHTML=`<div class="d-flex"><div class="toast-body">${text}</div><button class="btn-close btn-close-white me-2 m-auto" aria-label="Đóng"></button></div>`;document.querySelector("#toast-container").append(el);el.querySelector("button").onclick=()=>el.remove();setTimeout(()=>el.remove(),4000)}
